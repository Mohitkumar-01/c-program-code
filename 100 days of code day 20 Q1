// Finding the product of odd number
#include<stdio.h>
int main(){
    int i,n;
    long product=1;
    printf("Enter an Integer:");
    scanf("%d",&n);
        for(i=1;i<=n;i+=2){
    product*=i;}
    printf("Product of odd number up to %d is = %ld",n,product);
    return 0;
}