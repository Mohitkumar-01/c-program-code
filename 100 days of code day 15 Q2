//reversing a given number.
#include<stdio.h>
int main (){
    int remainder,n;
    printf("Enter the Number: ");
    scanf("%d",&n);
    while(n>0){
        remainder = n%10;
        printf("%d",remainder);
        n /= 10;
    }
    return 0;
}